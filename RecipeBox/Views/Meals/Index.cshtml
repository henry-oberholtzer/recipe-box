@{
  ViewData["Title"] = "Recipe Meal Types";
}
@using RecipeBox.Models;
<h1>Meals</h1>

@if (@Model.Count == 0)
{
  <h3>No recipe meal types have been added yet...</h3>
}
@foreach (Meal meal in Model)
{
  <h5>@Html.ActionLink($"{meal.Name}", "Details", new { id = meal.MealId })</h5>
}