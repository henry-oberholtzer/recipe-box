@model List<Ingredient>

<ul>
@if(Model.Count == 0)
{
  <li>No Ingredients Found</li>
}

@foreach (Ingredient i in Model)
{
  <li>@Html.ActionLink($"{i.Name} - Used in {i.IngredientRecipes.Count()} recipes", "Details", "Ingredients", new { id = i.IngredientId })</li>
}
</ul>
