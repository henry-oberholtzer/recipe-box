@model List<IngredientRecipe>

<ul>
@if(Model.Count == 0)
{
  <li>No Ingredients</li>
}
@foreach (IngredientRecipe i in Model)
{
  <li>@Enum.GetName(typeof(Units), i.Unit) @i.Amount @Html.ActionLink(i.Ingredient.Name, "Details", "Ingredients", new { id = i.IngredientId })</li>
}
</ul>
