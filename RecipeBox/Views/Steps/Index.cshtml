@{
  ViewData["Title"] = "Recipe Steps";
}

@using RecipeBox.Models

<h1>Steps</h1>
@if (@Model.Count == 0)
{
  <h3>There have been no steps added yet...</h3>
}
@foreach (Step step in Model)
{
  <h5>@Html.ActionLink($"{step.StepIndex}", "Details", new { id = step.StepId })</h5>
}