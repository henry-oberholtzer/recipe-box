@model List<Recipe>
@using RecipeBox.Models;

@if (@Model.Count == 0)
{
  <h3>No recipes have been added yet...</h3>
}
else
{
  <ul>
  @foreach (Recipe r in Model)
    {
      <li>
        <h3>@Html.ActionLink(r.Title, "Details", "Recipes", new { id = r.RecipeId })</h3>
      </li>
    }
  </ul>
}